export const HEALTH_PROVIDERS = ["apple_health", "google_health_connect"] as const;

// Maximum characters returned in a single tool response's text content.
export const CHARACTER_LIMIT = 25000;

export const DEFAULT_LIST_LIMIT = 20;
export const MAX_LIST_LIMIT = 200;
